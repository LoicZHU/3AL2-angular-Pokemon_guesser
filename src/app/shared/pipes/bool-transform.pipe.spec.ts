import { BoolTransformPipe } from './bool-transform.pipe';

describe('BoolTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
